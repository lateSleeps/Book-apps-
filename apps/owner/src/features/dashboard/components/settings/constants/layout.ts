// Settings layout constants.
// Import these in components instead of hardcoding values.

// Max content width for settings pages.
// Forms are hard to scan beyond this width on wide monitors.
export const SETTINGS_MAX_WIDTH = 'max-w-[1200px]';

// Vertical gap between top-level sections inside a settings page.
export const SETTINGS_SECTION_GAP = 'gap-s32';

// Vertical gap between cards inside a section.
export const SETTINGS_CARD_GAP = 'gap-s16';
