// Settings layout constants.
// Import these in components instead of hardcoding values.

// Vertical gap between the sub-nav and sections inside a settings page.
// Matches the Riwayat module rhythm.
export const SETTINGS_SECTION_GAP = 'gap-s24';

// Vertical gap between cards inside a section.
export const SETTINGS_CARD_GAP = 'gap-s16';

// Fixed width of the settings navigation panel on desktop.
export const SETTINGS_NAV_WIDTH = 'w-56';

// Min-height for nav panel items (touch target).
export const SETTINGS_NAV_ITEM_HEIGHT = 'min-h-[36px]';
